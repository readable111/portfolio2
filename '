'use client'
import styles from './bufferitem.module.css'

export const Bufferitem = (props : {}) => {
  return (
    <div className={`${styles.bufferitem} nav-item`} onClick = props.onClick>
    </div>
  )
}
